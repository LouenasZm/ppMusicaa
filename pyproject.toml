[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "ppMusicaa"
version = "0.1.0"
description = "My core musicaa postprocessing framework"
readme = "README.md"
requires-python = ">=3.9"
dependencies = [
    "numpy",
    # Add other dependencies ppMusicaa needs here
]

[tool.setuptools.packages.find]
where = ["."]
include = ["ppModule*"]  